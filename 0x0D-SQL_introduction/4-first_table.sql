-- 0x0D. SQL - Introduction, task 4. First table
-- Creates table `first_table` in database from args, if not already existing.
CREATE TABLE IF NOT EXISTS first_table (
       id INT,
       name VARCHAR(256)
);
