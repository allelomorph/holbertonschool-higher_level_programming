#!/usr/bin/node
// create class `Rectangle` that defines a rectangle, with input filtering
class Rectangle {
  constructor (w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
  }
}

module.exports = Rectangle;
