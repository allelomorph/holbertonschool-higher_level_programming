#!/usr/bin/node
// edit existing code (insert line 8) to replace 12 with 89
const myObject = {
  type: 'object',
  value: 12
};
console.log(myObject);
myObject.value = 89; // added this line
console.log(myObject);
