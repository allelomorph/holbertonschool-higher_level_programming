-- 0x0E. SQL - More queries, task 3. Always a name
-- Creates table `force_name` with `id and `name` fields. DB in args.
CREATE TABLE IF NOT EXISTS force_name (
       id INT,
       name VARCHAR(256) NOT NULL
); 
